enum ActionEnum {
  search = 'SEARCH',
  items = 'ITEMS',
  data = 'DATA',
  card = 'CARD',
  flagMain = 'FLAG_MAIN',
  flagCards = 'FLAG_CARDS',
  flagCard = 'FLAG_CARD',
  mode = 'VIEW_MODE',
}

export default ActionEnum;
